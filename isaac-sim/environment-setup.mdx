---
title: "Environment setup"
sidebarTitle: "Environment setup"
description: "Isaac Sim 5.1.0 conda + pip environment setup"
---

Miniforge 를 설치한 후 아래 가이드를 따라 환경을 설정합니다. 

### 1. Conda 환경 생성

```bash
conda create -n env_isaacsim python=3.11
conda activate env_isaacsim
pip install --upgrade pip
```

### 2. Isaac Sim 전체 패키지 설치

```bash
pip install uv 

uv pip install isaacsim[all,extscache]==5.1.0 --extra-index-url https://pypi.nvidia.com
```

### 3. EULA 수락

첫 실행 시 EULA 프롬프트가 뜨지 않게 하려면 환경 변수를 설정합니다.

```bash
export OMNI_KIT_ACCEPT_EULA=YES
```

### 4. 실행 확인

```bash
isaacsim
```